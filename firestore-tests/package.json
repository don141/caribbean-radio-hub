{
  "name": "breadfruit-firestore-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Breadfruit — Firestore security-rules emulator tests (BRE-38)",
  "scripts": {
    "test": "firebase emulators:exec --only firestore \"jest --runInBand\"",
    "test:jest": "jest --runInBand"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.4",
    "firebase": "^10.13.2",
    "firebase-tools": "^13.23.1",
    "jest": "^29.7.0"
  }
}
